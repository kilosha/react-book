import React from 'react';
import { Typography } from '@mui/material';

const VirtualDOM = () => {
    return (
        <div>
            <Typography paragraph>VirtualDOM</Typography>
            <Typography paragraph>VirtualDOM2</Typography>
        </div>
    );
};

export default VirtualDOM;
